package acc.healthapp.model;

/**
 * Current priority of the Request. Depending on the time and original priority this enum
 * represents the current status.
 */
public enum RequestCurrentPriority {

    LOW,
    MEDIUM,
    HIGH,
}
